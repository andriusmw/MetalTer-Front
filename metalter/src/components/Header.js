import { Auth } from "./Auth"

export const Header = () => {
    return (
        <header>
            <h1>\m/ MetalTer \m/</h1>
            <nav>
                <Auth />
           </nav>
        </header>
    )
}